# ESP32-C5 智能门控系统通信协议规范 v1.0

## 📚 目录
1. [协议架构](#1-协议架构)
2. [MQTT 协议规范](#2-mqtt-协议规范)
3. [BLE 协议规范](#3-ble-协议规范)
4. [统一控制指令格式](#4-统一控制指令格式)
5. [串口协议映射](#5-串口协议映射)
6. [错误码定义](#6-错误码定义)
7. [数据流示例](#7-数据流示例)

---

## 1. 协议架构

### 1.1 系统拓扑

```
┌─────────────┐         ┌──────────────┐
│   涂鸦云     │         │  手机 APP    │
│   (MQTT)    │         │   (BLE)      │
└──────┬──────┘         └──────┬───────┘
       │ MQTT over TLS         │ BLE GATT
       │ Port: 8883            │ UUID: 自定义
       │                       │
       └───────┬───────────────┘
               │
        ┌──────▼─────────┐
        │   ESP32-C5     │
        │   WiFi+BLE     │
        └──────┬─────────┘
               │ UART (115200,8N1)
        ┌──────▼─────────┐
        │   主控盒       │
        │  (门控设备)    │
        └────────────────┘
```

### 1.2 协议层次

| 层级 | MQTT 路径 | BLE 路径 | 说明 |
|------|----------|----------|------|
| **应用层** | JSON 格式 | JSON/HEX 格式 | 业务逻辑 |
| **传输层** | MQTT QoS 0/1 | GATT Write/Notify | 可靠传输 |
| **网络层** | TCP/IP | BLE L2CAP | 连接管理 |
| **串口层** | UART 协议 | UART 协议 | 最终输出 |

---

## 2. MQTT 协议规范

### 2.1 连接参数

| 参数 | 值 | 说明 |
|------|-----|-----|
| **Broker** | `mqtts://m1.tuyacn.com:8883` | 涂鸦中国区域 |
| **Client ID** | `tuyalink_{deviceId}` | 设备唯一标识 |
| **Username** | `{deviceId}|signMethod=hmacSha256,timestamp={ts},secureMode=1,accessType=1` | 动态生成 |
| **Password** | HMAC-SHA256 签名 | 根据 deviceSecret 计算 |
| **Keep Alive** | 60 秒 | 心跳间隔 |
| **Clean Session** | false | 保持会话 |
| **TLS** | 启用（CA证书验证） | 安全连接 |

### 2.2 Topic 定义

#### 2.2.1 订阅 Topic（下行：云 → 设备）

| Topic | QoS | 说明 |
|-------|-----|-----|
| `tylink/{deviceId}/thing/property/set` | 1 | 设备属性设置（控制指令） |
| `tylink/{deviceId}/thing/command/invoke` | 1 | 服务调用（预留） |

#### 2.2.2 发布 Topic（上行：设备 → 云）

| Topic | QoS | 说明 |
|-------|-----|-----|
| `tylink/{deviceId}/thing/property/report` | 1 | 设备属性上报（状态同步） |
| `tylink/{deviceId}/thing/event/trigger` | 1 | 事件上报（预留） |

### 2.3 消息格式

#### 2.3.1 控制指令（云 → 设备）

**Topic**: `tylink/{deviceId}/thing/property/set`

**Payload 格式**:

```json
{
    "msgId": "1234567890",
    "time": 1640000000000,
    "data": {
        "mccil": "0C0164000000002864",
        "type": "control"
    }
}
```

**字段说明**:

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `msgId` | String | 是 | 消息唯一标识（用于响应关联） |
| `time` | Long | 是 | 时间戳（毫秒） |
| `data.mccil` | String | 是 | **主控盒指令载荷**（十六进制字符串，无空格，大写） |
| `data.type` | String | 否 | 指令类型，默认 `"control"` |

**`mccil` 字段格式** (Main Controller Command Interface Layer):

```
格式: 十六进制字符串（每2个字符表示1字节）
示例: "0C0164000000002864"
解析后: [0x0C, 0x01, 0x64, 0x00, 0x00, 0x00, 0x00, 0x28, 0x64]
       │     │     │                             └─ 校验字节（可选）
       │     │     └─ 数据域（参数）
       │     └─ 功能码
       └─ 命令字（0x0C 为控制命令）
```

#### 2.3.2 状态上报（设备 → 云）

**Topic**: `tylink/{deviceId}/thing/property/report`

**Payload 格式**:

```json
{
    "msgId": "9876543210",
    "time": 1640000001000,
    "data": {
        "online": true,
        "deviceStatus": {
            "position": 100,
            "mode": 2,
            "alarm": 0,
            "motorCurrent": 1500,
            "runCount": 12345
        },
        "rawData": "140282641012C8006400000BB805DC0000304D02"
    }
}
```

**字段说明**:

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `msgId` | String | 是 | 消息唯一标识 |
| `time` | Long | 是 | 时间戳（毫秒） |
| `data.online` | Boolean | 是 | 设备在线状态 |
| `data.deviceStatus` | Object | 否 | 解析后的设备状态 |
| `data.deviceStatus.position` | Integer | 否 | 当前位置（0-65535） |
| `data.deviceStatus.mode` | Integer | 否 | 工作模式（0-127） |
| `data.deviceStatus.alarm` | Integer | 否 | 报警代码（0=正常） |
| `data.deviceStatus.motorCurrent` | Integer | 否 | 电机电流（mA） |
| `data.deviceStatus.runCount` | Integer | 否 | 运行次数 |
| `data.rawData` | String | 否 | 原始串口数据（十六进制，用于调试） |

**状态上报触发条件**:

1. 设备 MQTT 连接成功后，立即上报在线状态
2. 串口收到主控盒状态帧（0x82/0x85）时，解析后上报
3. 定期心跳上报（建议间隔：60秒）
4. 关键参数变化时（位置、模式、报警）

#### 2.3.3 应答消息（设备 → 云，可选）

对于需要确认的控制指令，设备可发送应答：

```json
{
    "msgId": "1234567890_ack",
    "time": 1640000000100,
    "data": {
        "requestId": "1234567890",
        "code": 0,
        "message": "success"
    }
}
```

**code 错误码**: 见 [第6章](#6-错误码定义)

---

## 3. BLE 协议规范

### 3.1 GATT 服务定义

#### 3.1.1 服务 UUID

```
服务 UUID (128-bit): 59462f12-9543-9999-12c8-58b459a2712d
服务名称: ESP32-C5 门控服务
```

#### 3.1.2 特征值定义

| 特征值名称 | UUID | 属性 | 说明 |
|-----------|------|------|------|
| **控制特征** | 33333333-2222-2222-2222-111111110000 | Read, Write | 接收控制指令和配网数据 |
| **状态特征** | 33333333-2222-2222-2222-111111110001 | Read, Notify | 推送设备状态（预留） |

### 3.2 数据传输

#### 3.2.1 MTU 协商

- **默认 MTU**: 23 字节（有效载荷 20 字节）
- **首选 MTU**: 500 字节（已在代码中配置）
- **实际 MTU**: 取决于手机端支持，连接后自动协商

#### 3.2.2 长数据处理

如果数据超过 MTU-3 字节：
- 方案1：APP 端分包发送，ESP32 端重组
- 方案2：使用 JSON 压缩格式
- 方案3：使用 BLE 长写入特性（部分手机支持）

### 3.3 消息格式

#### 3.3.1 控制指令（APP → 设备）

**写入特征值**: 控制特征 (UUID: ...0000)

**格式选项 A（推荐）**: JSON 格式

```json
{
    "cmd": "control",
    "data": "0C0164000000002864"
}
```

**格式选项 B**: 十六进制字符串（兼容当前实现）

```
直接写入: "0C0164000000002864"
```

**字段说明**:

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `cmd` | String | 是 | 命令类型：`"control"`（控制）或 `"config"`（配网） |
| `data` | String | 是 | 十六进制指令字符串（与 MQTT `mccil` 字段格式相同） |

#### 3.3.2 配网指令（APP → 设备）

**写入特征值**: 控制特征 (UUID: ...0000)

**格式**: JSON

```json
{
    "cmd": "config",
    "data": {
        "ssid": "MyWiFi",
        "password": "12345678",
        "region": "cn",
        "token": "a1b2c3d4e5f6"
    }
}
```

**字段说明**:

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `cmd` | String | 是 | 固定值 `"config"` |
| `data.ssid` | String | 是 | WiFi SSID（最大32字节） |
| `data.password` | String | 是 | WiFi 密码（最大64字节） |
| `data.region` | String | 否 | 涂鸦区域（cn/us/eu），默认 cn |
| `data.token` | String | 否 | 涂鸦配网 Token（从云端获取） |

#### 3.3.3 状态推送（设备 → APP）

**Notify 特征值**: 状态特征 (UUID: ...0001)

**格式**: JSON

```json
{
    "event": "status",
    "data": {
        "wifi": "connected",
        "ip": "192.168.1.100",
        "mqtt": "connected",
        "position": 100,
        "mode": 2
    }
}
```

**事件类型**:

| event | 说明 | 触发时机 |
|-------|------|---------|
| `status` | 设备状态 | 状态变化时 |
| `config_result` | 配网结果 | 配网成功/失败 |
| `error` | 错误通知 | 发生错误时 |

#### 3.3.4 配网结果推送

```json
{
    "event": "config_result",
    "data": {
        "result": "success",
        "ip": "192.168.1.100",
        "mqtt": "connected",
        "message": "设备已连接到云端"
    }
}
```

**result 取值**:

- `"success"`: 配网成功，已连接云端
- `"wifi_failed"`: WiFi 连接失败
- `"mqtt_failed"`: MQTT 连接失败
- `"timeout"`: 配网超时

---

## 4. 统一控制指令格式

### 4.1 指令结构

MQTT 和 BLE 的控制指令**完全相同**，都使用十六进制字符串格式：

```
格式: [命令字][功能码][数据域][校验]
长度: 可变（通常 6-64 字节）
编码: 十六进制字符串（大写，无空格，无 0x 前缀）
```

### 4.2 命令字定义

根据 `use_uart.c` 当前实现，支持的命令字：

| 命令字 | 名称 | 说明 | 数据长度 |
|--------|------|------|----------|
| `0x0C` | 控制命令 | 发送给主控盒的操作指令 | 可变（通常9字节） |

**未来扩展命令字（预留）**:

| 命令字 | 名称 | 说明 |
|--------|------|------|
| `0x01` | 查询状态 | 查询主控盒当前状态 |
| `0x02` | 读寄存器 | 读取配置寄存器 |
| `0x03` | 写寄存器 | 写入配置寄存器 |
| `0xFE` | 固件升级 | OTA 相关指令（预留） |
| `0xFF` | 系统命令 | ESP32 内部命令（不转发串口） |

### 4.3 0x0C 控制命令详细格式

```
字节索引:  0     1     2     3     4     5     6     7     8
         [0x0C][Func][Param1        ][Param2        ][CRC ]
示例:     0C    01    64    00    00    00    00    28    64
```

| 字节 | 名称 | 说明 | 取值范围 |
|------|------|------|----------|
| 0 | 命令字 | 固定 0x0C | 0x0C |
| 1 | 功能码 | 子命令 | 0x01-0xFF |
| 2-5 | 参数1 | 具体参数（大端序） | 根据功能码定义 |
| 6-7 | 参数2 | 具体参数（大端序） | 根据功能码定义 |
| 8 | 校验和 | CRC8 或简单校验 | 计算得出 |

**功能码示例** (根据主控盒文档定义):

| 功能码 | 名称 | 参数1 | 参数2 | 说明 |
|--------|------|-------|-------|------|
| 0x01 | 设置位置 | 目标位置(0-100) | 速度 | 门移动到指定位置 |
| 0x02 | 停止 | - | - | 立即停止 |
| 0x10 | 开门 | - | - | 完全打开 |
| 0x11 | 关门 | - | - | 完全关闭 |
| 0x12 | 暂停 | - | - | 暂停当前动作 |

### 4.4 指令示例

#### 示例1: 开门指令

```
原始指令: 0x0C 0x10 0x00 0x00 0x00 0x00 0x00 0x00 0x1C
MQTT JSON:
{
    "data": {
        "mccil": "0C1000000000000000001C"
    }
}

BLE JSON:
{
    "cmd": "control",
    "data": "0C1000000000000000001C"
}

BLE 直接写入: "0C1000000000000000001C"
```

#### 示例2: 设置位置到50%

```
原始指令: 0x0C 0x01 0x00 0x00 0x00 0x32 0x00 0x05 0x44
解析:
  0x0C - 控制命令
  0x01 - 设置位置功能
  0x00000032 - 位置值 50 (十进制)
  0x0005 - 速度 5
  0x44 - 校验和

十六进制字符串: "0C010000003200050044"
```

---

## 5. 串口协议映射

### 5.1 下行（ESP32 → 主控盒）

| 来源 | MQTT/BLE 指令 | 串口输出 | 转换规则 |
|------|--------------|---------|---------|
| MQTT | `"mccil": "0C01..."` | `[0x0C, 0x01, ...]` | 十六进制字符串 → 字节数组 |
| BLE  | `"data": "0C01..."` | `[0x0C, 0x01, ...]` | 十六进制字符串 → 字节数组 |

**转换代码逻辑（参考）**:

```c
// 每2个十六进制字符转换为1个字节
"0C" → 0x0C
"01" → 0x01
"64" → 0x64
```

### 5.2 上行（主控盒 → ESP32）

| 串口接收 | 帧类型 | MQTT 上报 | BLE Notify |
|---------|--------|-----------|-----------|
| `[0x14, 0x82, ...]` | 键值应答 (0x82) | 解析后上报 JSON | 原始十六进制 |
| `[0x14, 0x85, ...]` | 指令应答 (0x85) | 解析后上报 JSON | 原始十六进制 |
| `[0x06, 0x83, ...]` | 读应答 (0x83) | 解析后上报 JSON | 原始十六进制 |
| `[0x06, 0x84, ...]` | 写应答 (0x84) | 解析后上报 JSON | 原始十六进制 |

**串口帧结构** (详见 `use_uart.h`):

```
长度字段: 0x14 (20字节) 或 0x06 (6字节)
类型字段: 0x82/0x85/0x83/0x84
数据域: 根据类型不同而不同
```

---

## 6. 错误码定义

### 6.1 通用错误码

| Code | 名称 | 说明 | 处理建议 |
|------|------|------|---------|
| 0 | SUCCESS | 成功 | - |
| -1 | UNKNOWN_ERROR | 未知错误 | 重试 |
| -2 | INVALID_PARAM | 参数错误 | 检查参数格式 |
| -3 | TIMEOUT | 操作超时 | 检查网络/串口连接 |
| -4 | NOT_CONNECTED | 未连接 | 检查 WiFi/MQTT/BLE 状态 |

### 6.2 MQTT 特定错误码

| Code | 名称 | 说明 |
|------|------|------|
| 1001 | MQTT_NOT_READY | MQTT 未连接 |
| 1002 | JSON_PARSE_ERROR | JSON 解析失败 |
| 1003 | MCCIL_FIELD_MISSING | mccil 字段缺失 |
| 1004 | HEX_CONVERT_ERROR | 十六进制转换失败 |

### 6.3 BLE 特定错误码

| Code | 名称 | 说明 |
|------|------|------|
| 2001 | BLE_NOT_CONNECTED | BLE 未连接 |
| 2002 | MTU_TOO_SMALL | MTU 不足，数据过大 |
| 2003 | WIFI_CONFIG_FAILED | WiFi 配网失败 |

### 6.4 串口错误码

| Code | 名称 | 说明 |
|------|------|------|
| 3001 | UART_NOT_INITIALIZED | 串口未初始化 |
| 3002 | UART_SEND_FAILED | 串口发送失败 |
| 3003 | INVALID_FRAME | 非法帧格式 |
| 3004 | UNKNOWN_COMMAND | 未识别的命令字 |

---

## 7. 数据流示例

### 7.1 MQTT 控制流程

```
┌─────────┐                  ┌──────────┐                ┌─────────┐
│ 涂鸦云   │                  │ ESP32-C5 │                │ 主控盒  │
└────┬────┘                  └─────┬────┘                └────┬────┘
     │                             │                          │
     │ 1. Publish                  │                          │
     │ Topic: .../property/set     │                          │
     │ {"data":{"mccil":"0C01..."}}│                          │
     ├────────────────────────────>│                          │
     │                             │                          │
     │                             │ 2. 解析 JSON             │
     │                             │    提取 mccil 字段       │
     │                             │                          │
     │                             │ 3. 十六进制 → 字节       │
     │                             │    "0C01..." → [0x0C, 0x01, ...]
     │                             │                          │
     │                             │ 4. UART 发送             │
     │                             ├─────────────────────────>│
     │                             │    [0x0C, 0x01, ...]     │
     │                             │                          │
     │                             │                          │ 5. 处理指令
     │                             │                          │    执行动作
     │                             │                          │
     │                             │ 6. UART 应答             │
     │                             │<─────────────────────────┤
     │                             │    [0x14, 0x85, ...]     │
     │                             │                          │
     │                             │ 7. 解析串口帧            │
     │                             │    提取状态信息          │
     │                             │                          │
     │ 8. Publish                  │                          │
     │ Topic: .../property/report  │                          │
     │<────────────────────────────┤                          │
     │ {"data":{"deviceStatus":{...}}}                        │
     │                             │                          │
```

### 7.2 BLE 配网流程

```
┌─────────┐                  ┌──────────┐                ┌─────────┐
│ 手机APP │                  │ ESP32-C5 │                │  路由器 │
└────┬────┘                  └─────┬────┘                └────┬────┘
     │                             │                          │
     │ 1. BLE 扫描并连接           │                          │
     ├────────────────────────────>│                          │
     │                             │                          │
     │ 2. 写入配网信息             │                          │
     │ {"cmd":"config",            │                          │
     │  "data":{"ssid":"...","password":"..."}}              │
     ├────────────────────────────>│                          │
     │                             │                          │
     │                             │ 3. 解析 WiFi 信息        │
     │                             │                          │
     │                             │ 4. 连接 WiFi             │
     │                             ├─────────────────────────>│
     │                             │                          │
     │                             │ 5. 获取 IP               │
     │                             │<─────────────────────────┤
     │                             │                          │
     │ 6. Notify 配网结果          │                          │
     │<────────────────────────────┤                          │
     │ {"event":"config_result",   │                          │
     │  "data":{"result":"success"}}                          │
     │                             │                          │
     │ 7. 断开 BLE（可选）         │                          │
     ├────────────────────────────>│                          │
     │                             │                          │
     │                             │ 8. 连接 MQTT             │
     │                             │ (后续走云端通信)         │
     │                             │                          │
```

### 7.3 BLE 控制流程

```
┌─────────┐                  ┌──────────┐                ┌─────────┐
│ 手机APP │                  │ ESP32-C5 │                │ 主控盒  │
└────┬────┘                  └─────┬────┘                └────┬────┘
     │                             │                          │
     │ 1. 写入控制指令             │                          │
     │ {"cmd":"control",           │                          │
     │  "data":"0C01..."}          │                          │
     ├────────────────────────────>│                          │
     │                             │                          │
     │                             │ 2. 与 MQTT 相同的处理    │
     │                             │    十六进制 → 字节       │
     │                             │                          │
     │                             │ 3. UART 发送             │
     │                             ├─────────────────────────>│
     │                             │                          │
     │                             │ 4. UART 应答             │
     │                             │<─────────────────────────┤
     │                             │                          │
     │ 5. Notify 状态（可选）      │                          │
     │<────────────────────────────┤                          │
     │ {"event":"status",          │                          │
     │  "data":{"position":50}}    │                          │
     │                             │                          │
```

---

## 8. 安全机制

### 8.1 MQTT 安全

| 机制 | 实现 | 说明 |
|------|------|------|
| **传输加密** | TLS 1.2+ | 使用 CA 证书验证 |
| **身份认证** | HMAC-SHA256 | 基于 deviceSecret 签名 |
| **时间戳** | Unix 时间戳 | 防止重放攻击 |
| **权限隔离** | Topic 订阅权限 | 设备只能访问自己的 Topic |

### 8.2 BLE 安全（可选）

| 机制 | 实现 | 说明 |
|------|------|------|
| **配对** | Just Works / Passkey | 防止未授权连接 |
| **加密** | BLE Security Level 2 | 链路层加密 |
| **超时** | 配网完成后断开 | 减少攻击窗口 |

### 8.3 数据校验

1. **MQTT JSON 校验**: 必须包含 `msgId`, `time`, `data` 字段
2. **十六进制格式校验**: 只允许 0-9, A-F 字符，长度必须是偶数
3. **命令字白名单**: 只允许 0x0C 等已定义的命令字
4. **参数范围校验**: 位置 0-100，速度 0-9 等

---

## 9. 性能指标

### 9.1 延迟

| 场景 | 典型值 | 最大值 | 说明 |
|------|--------|--------|------|
| MQTT 命令下发 | 200ms | 2s | 取决于网络质量 |
| BLE 命令下发 | 50ms | 500ms | 取决于 MTU 和连接间隔 |
| 串口响应 | 10ms | 100ms | 主控盒处理时间 |
| 状态上报 | 100ms | 1s | 串口 → 云端 |

### 9.2 吞吐量

| 通道 | 吞吐量 | 说明 |
|------|--------|------|
| MQTT | 10 条/秒 | 建议不超过此频率 |
| BLE | 20 条/秒 | MTU=500 时 |
| UART | 100 条/秒 | 115200 波特率 |

### 9.3 队列容量

| 队列 | 深度 | 单条大小 | 总容量 |
|------|------|----------|--------|
| `g_msg_queue` | 10 | 256 字节 | 2.5 KB |
| UART RX Buffer | 1024 | 1 字节 | 1 KB |
| MQTT Buffer | - | 1024 字节 | 动态分配 |

---

## 10. 兼容性

### 10.1 向后兼容

- BLE 支持**纯十六进制字符串**格式（当前实现）
- BLE 同时支持**JSON 格式**（推荐，未来默认）
- 使用 `cmd` 字段区分，缺失则默认为控制指令

### 10.2 版本演进

| 版本 | 变化 | 兼容性 |
|------|------|--------|
| v1.0 | 初始版本（当前） | - |
| v1.1 | BLE 增加 JSON 支持 | 向后兼容 |
| v1.2 | 增加命令字 0x01-0x03 | 向后兼容 |
| v2.0 | 修改串口协议 | **不兼容** |

---

## 11. 开发建议

### 11.1 云端开发者

1. **遵循涂鸦云 API 规范**，使用标准 TuyaLink 协议
2. **mccil 字段命名**建议改为更通用的名称（如 `hexCommand`）
3. **时间戳校验**：云端下发时间戳，设备检查是否在±5分钟内
4. **重试机制**：QoS=1 确保消息送达，超时重发

### 11.2 APP 开发者（BLE）

1. **MTU 协商**：连接后立即请求 MTU 交换，尽量获取 200+ 字节
2. **分包策略**：如果 MTU 小于数据长度，需要应用层分包
3. **配网超时**：建议设置 30 秒超时，超时后提示用户重试
4. **断线重连**：配网成功后可断开 BLE，节省手机功耗

### 11.3 ESP32 固件开发者

1. **消息队列监控**：定期检查队列使用率，超过 80% 时打印警告
2. **日志分级**：正常流程用 INFO，错误用 ERROR，调试用 DEBUG
3. **内存管理**：使用 `heap_caps_get_free_size()` 监控剩余内存
4. **错误恢复**：MQTT 断线后自动重连，WiFi 断开后尝试重连

### 11.4 测试建议

#### 功能测试
- MQTT 下发指令 → 串口输出正确
- BLE 下发指令 → 串口输出正确
- BLE 配网 → WiFi 连接成功
- 串口应答 → MQTT 上报正确

#### 压力测试
- MQTT 连续发送 100 条指令，检查丢包率
- BLE 和 MQTT 同时发送，检查队列溢出
- 长时间运行（24小时），检查内存泄漏

#### 异常测试
- 网络断开后恢复
- 串口错误数据处理
- JSON 格式错误处理
- 十六进制非法字符处理

---

## 12. 附录

### 12.1 完整示例

#### MQTT 控制示例（Python）

```python
import paho.mqtt.client as mqtt
import json
import time

def on_connect(client, userdata, flags, rc):
    print(f"Connected with result code {rc}")
    # 订阅状态上报
    client.subscribe("tylink/2631a16994c01c1f45qfha/thing/property/report")

def on_message(client, userdata, msg):
    print(f"Received: {msg.topic} {msg.payload}")

client = mqtt.Client()
client.on_connect = on_connect
client.on_message = on_message

# 连接（需要提前计算 username/password）
client.connect("m1.tuyacn.com", 8883, 60)
client.loop_start()

# 发送开门指令
control_msg = {
    "msgId": str(int(time.time() * 1000)),
    "time": int(time.time() * 1000),
    "data": {
        "mccil": "0C1000000000000000001C"
    }
}
client.publish(
    "tylink/2631a16994c01c1f45qfha/thing/property/set",
    json.dumps(control_msg)
)

time.sleep(10)
client.loop_stop()
```

#### BLE 控制示例（JavaScript - Web Bluetooth）

```javascript
// 连接 BLE 设备
const device = await navigator.bluetooth.requestDevice({
    filters: [{ name: 'ESP32C5_TETS_1' }],
    optionalServices: ['59462f12-9543-9999-12c8-58b459a2712d']
});

const server = await device.gatt.connect();
const service = await server.getPrimaryService('59462f12-9543-9999-12c8-58b459a2712d');
const characteristic = await service.getCharacteristic('33333333-2222-2222-2222-111111110000');

// 发送控制指令
const command = {
    cmd: "control",
    data: "0C1000000000000000001C"
};
const encoder = new TextEncoder();
await characteristic.writeValue(encoder.encode(JSON.stringify(command)));

// 发送配网指令
const configCommand = {
    cmd: "config",
    data: {
        ssid: "MyWiFi",
        password: "12345678"
    }
};
await characteristic.writeValue(encoder.encode(JSON.stringify(configCommand)));
```

### 12.2 参考文档

- 涂鸦 TuyaLink 协议: https://developer.tuya.com/docs/iot/tuyalink-intro
- ESP32-C5 BLE 开发指南: https://docs.espressif.com/projects/esp-idf/
- 主控盒串口协议: 见《控制系统 RF&BLE 通信协议 V1.14.docx》

### 12.3 变更历史

| 版本 | 日期 | 作者 | 变更内容 |
|------|------|------|---------|
| v1.0 | 2025-10-22 | AI Assistant | 初始版本 |

---

**文档结束**

