# MQTT 与 BLE 协议对比分析

## 📊 协议特性对比

| 特性维度 | MQTT | BLE | 说明 |
|---------|------|-----|------|
| **传输层** | TCP/IP over WiFi | L2CAP over BLE 4.2+ | MQTT 依赖网络，BLE 直连 |
| **连接范围** | 无限（通过互联网） | 10-50m（视障碍物） | MQTT 可远程，BLE 仅近场 |
| **功耗** | 中等（WiFi 常开） | 低（BLE 低功耗） | BLE 适合电池设备 |
| **数据量** | 大（KB级） | 小（字节级，受MTU限制） | MQTT 适合批量数据 |
| **延迟** | 200ms（网络影响大） | 50ms（连接间隔影响） | BLE 更实时 |
| **安全性** | TLS 1.2 + HMAC 认证 | 可选配对和加密 | MQTT 安全性更高 |
| **配网能力** | ❌ 无（需要先有网络） | ✅ 有（可传输WiFi信息） | BLE 是配网入口 |

---

## 🎯 功能对比

### 共同功能（控制指令）

| 功能 | MQTT 实现 | BLE 实现 | 最终输出 |
|------|----------|----------|---------|
| 开门指令 | JSON 包含 `"mccil": "0C10..."` | JSON/直接写入 `"0C10..."` | UART: `[0x0C, 0x10, ...]` |
| 关门指令 | JSON 包含 `"mccil": "0C11..."` | JSON/直接写入 `"0C11..."` | UART: `[0x0C, 0x11, ...]` |
| 设置位置 | JSON 包含 `"mccil": "0C01..."` | JSON/直接写入 `"0C01..."` | UART: `[0x0C, 0x01, ...]` |

**结论**: ✅ **控制指令格式 100% 相同，可复用处理逻辑**

---

### 独有功能对比

| 功能 | MQTT | BLE | 原因 |
|------|------|-----|------|
| **远程控制** | ✅ 支持 | ❌ 不支持 | MQTT 通过互联网，BLE 仅近场 |
| **WiFi 配网** | ❌ 不支持 | ✅ 支持 | MQTT 需要先连网，BLE 可传配网信息 |
| **设备发现** | ❌ 需要提前知道 deviceId | ✅ 支持扫描 | BLE 广播可被发现 |
| **离线控制** | ❌ 需要网络 | ✅ 局域网可用 | BLE 直连，不依赖外网 |
| **云端联动** | ✅ 支持（与其他设备联动） | ❌ 不支持 | 只有 MQTT 可对接云端场景 |

---

## 🔄 数据流对比

### MQTT 控制流程（6步）

```
1. 云端下发控制指令
   ├─ Topic: tylink/{deviceId}/thing/property/set
   └─ Payload: {"data": {"mccil": "0C01..."}}

2. ESP32 接收 MQTT 消息
   └─ mqtt_event_handler() → MQTT_EVENT_DATA

3. 解析 JSON，提取 mccil 字段
   └─ cJSON_GetObjectItem(data, "mccil")

4. 放入统一消息队列
   └─ msg.source = MSG_SOURCE_MQTT
   └─ xQueueSend(g_msg_queue, &msg, ...)

5. 队列处理任务处理
   └─ mqtt_ble_data_parser_cb(&msg)
   └─ 十六进制字符串 → 字节数组

6. 串口发送给主控盒
   └─ uart_send_data(data, len)
```

### BLE 控制流程（5步）

```
1. APP 写入 GATT 特征值
   ├─ UUID: 33333333-2222-2222-2222-111111110000
   └─ Data: {"cmd": "control", "data": "0C01..."}
          或直接: "0C01..."

2. ESP32 接收 BLE 写入
   └─ gatt_svr_chr_access() → BLE_GATT_ACCESS_OP_WRITE_CHR

3. 放入统一消息队列
   └─ msg.source = MSG_SOURCE_BLE
   └─ xQueueSend(g_msg_queue, &msg, ...)

4. 队列处理任务处理
   └─ mqtt_ble_data_parser_cb(&msg)
   └─ 十六进制字符串 → 字节数组

5. 串口发送给主控盒
   └─ uart_send_data(data, len)
```

**关键差异**:
- MQTT 多了**解析 JSON** 步骤（步骤3）
- BLE 可以直接写入十六进制，或用 JSON（更灵活）
- 从步骤4开始，**两者完全相同**

---

## 📦 数据格式对比

### MQTT 数据格式（固定 JSON）

```json
{
    "msgId": "必填，消息ID",
    "time": "必填，时间戳（毫秒）",
    "data": {
        "mccil": "必填，十六进制字符串",
        "type": "可选，默认 'control'"
    }
}
```

**字段约束**:
- `msgId`: 字符串，用于追踪和应答
- `time`: 长整型，建议服务端校验时间窗口（±5分钟）
- `mccil`: 字符串，只允许 `0-9A-F`，长度必须是偶数

**示例**:
```json
{
    "msgId": "1730000000123",
    "time": 1730000000000,
    "data": {
        "mccil": "0C0164000000002864"
    }
}
```

---

### BLE 数据格式（两种选择）

#### 方式1: JSON 格式（推荐）

**控制指令**:
```json
{
    "cmd": "control",
    "data": "0C0164000000002864"
}
```

**配网指令**:
```json
{
    "cmd": "config",
    "data": {
        "ssid": "WiFi名称",
        "password": "WiFi密码"
    }
}
```

**字段约束**:
- `cmd`: 枚举 `"control"` | `"config"`
- `data`: 控制时为十六进制字符串，配网时为对象
- `ssid`: 最大 32 字节（UTF-8）
- `password`: 最大 64 字节（UTF-8）

---

#### 方式2: 纯十六进制（兼容模式）

```
直接写入: "0C0164000000002864"
```

**识别规则**:
- 如果第一个字符是 `{`，则按 JSON 解析
- 否则按纯十六进制字符串处理

---

## 🔧 ESP32 端处理对比

### MQTT 消息处理

```c
// mqtt_manager.c: MQTT 事件回调
case MQTT_EVENT_DATA:
    mqtt_manager_data_t data = {
        .topic = event->topic,
        .data = event->data,
        .data_len = event->data_len
    };
    status_callback(MQTT_MANAGER_EVENT_DATA_RECEIVED, &data);
    break;

// app_main.c: MQTT 回调
case MQTT_MANAGER_EVENT_DATA_RECEIVED:
    // 1. 解析 JSON
    root = cJSON_ParseWithLength(data->data, data->data_len);
    
    // 2. 提取 mccil 字段
    secondItem = cJSON_GetObjectItem(root, "data");
    mccil_item = cJSON_GetObjectItem(secondItem, "mccil");
    
    // 3. 放入队列
    g_msg_queue_t msg = {
        .source = MSG_SOURCE_MQTT,
        .type = MSG_TYPE_CONTROL,  // ★新增
        .data_len = strlen(mccil_item->valuestring)
    };
    memcpy(msg.data, mccil_item->valuestring, msg.data_len);
    xQueueSend(g_msg_queue, &msg, portMAX_DELAY);
    break;
```

---

### BLE 消息处理

```c
// use_ble_server.c: GATT 写入回调
case BLE_GATT_ACCESS_OP_WRITE_CHR:
    // 1. 接收数据
    received_len = OS_MBUF_PKTLEN(ctxt->om);
    ble_hs_mbuf_to_flat(ctxt->om, received_data, sizeof(received_data), &received_len);
    
    // 2. 判断格式（新增逻辑）★
    g_msg_queue_t msg;
    msg.source = MSG_SOURCE_BLE;
    
    if (received_data[0] == '{') {
        // JSON 格式
        cJSON *root = cJSON_Parse((char*)received_data);
        cJSON *cmd = cJSON_GetObjectItem(root, "cmd");
        
        if (strcmp(cmd->valuestring, "config") == 0) {
            msg.type = MSG_TYPE_BLE_CONFIG;  // ★配网
            // 提取 WiFi 信息...
        } else {
            msg.type = MSG_TYPE_CONTROL;  // ★控制
            cJSON *data = cJSON_GetObjectItem(root, "data");
            memcpy(msg.data, data->valuestring, ...);
        }
    } else {
        // 兼容模式：纯十六进制
        msg.type = MSG_TYPE_CONTROL;
        memcpy(msg.data, received_data, received_len);
    }
    
    // 3. 放入队列
    xQueueSend(g_msg_queue, &msg, 0);
    break;
```

---

### 统一处理逻辑

```c
// app_main.c: 统一消息处理
void mqtt_ble_data_parser_cb(const g_msg_queue_t* msg)
{
    const char* source_str = (msg->source == MSG_SOURCE_BLE) ? "BLE" : "MQTT";
    
    // ★根据 type 分发
    if (msg->type == MSG_TYPE_BLE_CONFIG) {
        // 处理配网
        ESP_LOGI(TAG, "收到 BLE 配网请求");
        parse_wifi_config_and_connect(msg->data);
        return;
    }
    
    // 处理控制指令（MQTT 和 BLE 统一逻辑）
    ESP_LOGI(TAG, "收到 %s 控制消息", source_str);
    
    // 1. 十六进制字符串 → 字节数组
    uint8_t data[64] = {0};
    uint8_t cmd_len = hex_string_to_bytes(msg->data, msg->data_len, data);
    
    // 2. 判断命令字
    if (data[0] == 0x0C) {
        // 3. 串口发送
        uart_send_data(data, cmd_len);
    } else {
        ESP_LOGW(TAG, "未识别的命令字: 0x%02X", data[0]);
    }
}
```

---

## 🎨 架构设计对比

### 方案A: 双队列（不推荐）

```
┌─────────┐         ┌─────────┐
│  MQTT   │         │   BLE   │
└────┬────┘         └────┬────┘
     │                   │
     │                   │
┌────▼─────┐       ┌────▼─────┐
│ mqtt_    │       │ ble_     │
│ queue    │       │ queue    │
│ 10条     │       │ 10条     │
└────┬─────┘       └────┬─────┘
     │                   │
┌────▼─────────────┬────▼─────┐
│ mqtt_task (6KB)  │ ble_task │
│                  │  (6KB)   │
└────┬─────────────┴────┬─────┘
     │                   │
     └──────┬────────────┘
            │
      ┌─────▼─────┐
      │   UART    │
      └───────────┘

内存占用: 约 17KB (5KB队列 + 12KB任务栈)
```

**缺点**:
- ❌ 内存占用大
- ❌ 两个任务调度开销
- ❌ 可能出现乱序（MQTT 和 BLE 消息处理速度不同）
- ❌ 代码复杂度高

---

### 方案B: 单队列 + type 字段（✅ 推荐）

```
┌─────────┐         ┌─────────┐
│  MQTT   │         │   BLE   │
└────┬────┘         └────┬────┘
     │ type=         │ type=
     │ CONTROL       │ CONTROL/CONFIG
     └─────┬─────────┘
           │
     ┌─────▼──────┐
     │ g_msg_     │
     │ queue      │
     │ 10条 2.5KB │
     └─────┬──────┘
           │
   ┌───────▼───────────┐
   │ message_task (6KB)│
   │ 根据 type 分发    │
   └───────┬───────────┘
           │
     ┌─────▼─────┐
     │ type判断  │
     │ ├─CONFIG  │
     │ │  └→WiFi │
     │ └─CONTROL │
     │    └→UART │
     └───────────┘

内存占用: 约 8.5KB (2.5KB队列 + 6KB任务栈)
```

**优点**:
- ✅ 节省约 8.5KB 内存
- ✅ 单任务，调度简单
- ✅ 保证消息有序处理
- ✅ 代码复用性高

---

## 📈 性能对比

### 延迟分析

| 场景 | MQTT | BLE | 串口输出 |
|------|------|-----|---------|
| **本地控制** | 不适用 | 50ms | 10ms |
| **远程控制** | 200ms（国内） | 不适用 | 10ms |
| **跨国控制** | 500ms-2s | 不适用 | 10ms |
| **配网** | 不适用 | 2-5秒 | - |

**结论**: 
- 本地控制用 BLE 更快（60ms vs 210ms）
- 远程控制只能用 MQTT
- 串口是最终瓶颈（10ms），不会成为性能瓶颈

---

### 吞吐量分析

| 通道 | 理论吞吐 | 实际吞吐 | 限制因素 |
|------|---------|---------|---------|
| **MQTT** | 100条/秒 | 10条/秒 | 网络延迟和 QoS |
| **BLE** | 50条/秒 | 20条/秒 | MTU 和连接间隔 |
| **UART** | 1000条/秒 | 100条/秒 | 115200 波特率 |
| **队列** | 100条/秒 | 50条/秒 | 队列深度10 |

**瓶颈分析**:
1. **正常场景**: MQTT 10条/秒，完全够用（人工控制频率 < 1条/秒）
2. **突发场景**: 队列可缓冲 10 条，能应对短时突发
3. **极端场景**: 如果 MQTT+BLE 同时高频发送，队列可能溢出（需要限流）

---

## 🔒 安全性对比

### MQTT 安全机制

| 机制 | 实现方式 | 安全等级 |
|------|---------|---------|
| **传输加密** | TLS 1.2 + CA 证书 | ⭐⭐⭐⭐⭐ |
| **身份认证** | HMAC-SHA256 签名 | ⭐⭐⭐⭐⭐ |
| **防重放** | 时间戳校验 | ⭐⭐⭐⭐ |
| **权限控制** | Topic 订阅权限 | ⭐⭐⭐⭐ |

**风险点**:
- ❌ 如果 `deviceSecret` 泄露，攻击者可伪造设备
- ✅ 建议定期轮换密钥（涂鸦云支持）

---

### BLE 安全机制

| 机制 | 实现方式 | 安全等级 |
|------|---------|---------|
| **传输加密** | BLE Security Level 2 | ⭐⭐⭐ |
| **身份认证** | 配对（Passkey/Just Works） | ⭐⭐ |
| **防重放** | 无（应用层需实现） | ⭐ |
| **权限控制** | 物理距离限制 | ⭐⭐⭐ |

**风险点**:
- ❌ 近场攻击（10m 内可被扫描到）
- ❌ 配对过程可能被中间人攻击（Just Works 模式）
- ✅ 建议配网完成后断开 BLE，只保留 MQTT

**安全建议**:
1. 配网完成后，立即关闭 BLE 广播
2. 如需保留 BLE，启用 Passkey 配对（6位PIN码）
3. 定期检查已配对设备列表，删除不明设备

---

## 🎯 使用场景建议

### 场景1: 首次配网

```
用户动作: 手机 APP 蓝牙连接设备
推荐方案: BLE
流程: 
  1. APP 扫描蓝牙设备
  2. 连接并写入 WiFi 信息（cmd=config）
  3. 设备连接 WiFi 和 MQTT
  4. 通过 BLE Notify 返回配网结果
  5. 断开 BLE（可选）
```

---

### 场景2: 局域网控制（在家）

```
用户动作: 打开门/调整位置
推荐方案: BLE（更快）或 MQTT（更稳定）
对比:
  - BLE: 延迟 50ms，不依赖外网，但需要近场
  - MQTT: 延迟 200ms，需要外网，但不受距离限制
建议: 优先 BLE，失败回退 MQTT
```

---

### 场景3: 远程控制（外出）

```
用户动作: 远程开门给快递员
推荐方案: MQTT（唯一选择）
流程:
  1. APP 通过涂鸦云下发指令
  2. 云端转发到 MQTT
  3. 设备执行并上报状态
  4. APP 收到云端推送的状态更新
```

---

### 场景4: 设备状态同步

```
用户动作: 查看门当前位置
推荐方案: MQTT（持久化）
原因:
  - MQTT 可持久化最新状态到云端
  - 多设备（手机、平板）可同步查看
  - BLE 只能单对单，不适合多设备
```

---

### 场景5: 自动化联动

```
用户动作: 回家自动开门（与其他智能设备联动）
推荐方案: MQTT（唯一选择）
原因:
  - 需要云端场景引擎协调多个设备
  - BLE 无法对接云端自动化
```

---

## 📌 关键结论

### ✅ 控制指令：完全统一

```
MQTT: {"data": {"mccil": "0C01..."}}
BLE:  {"cmd": "control", "data": "0C01..."}
      或直接: "0C01..."

最终都转换为: [0x0C, 0x01, ...] → UART 发送
```

**设计要点**:
- 使用**单队列**处理，节省资源
- 增加 **type 字段**区分配网和控制
- **复用** `mqtt_ble_data_parser_cb()` 处理逻辑

---

### ✅ 配网功能：BLE 独有

```
BLE: {"cmd": "config", "data": {"ssid": "...", "password": "..."}}

处理: 
  1. 判断 type == MSG_TYPE_BLE_CONFIG
  2. 提取 SSID 和密码
  3. 调用 wifi_manager_connect()
  4. 通过 BLE Notify 返回结果
```

**设计要点**:
- 配网数据**不转十六进制**，直接 JSON
- 配网成功后可选择**断开 BLE**（安全）
- 失败时通过 Notify 返回错误码

---

### ✅ 架构选择：单队列 + type

```c
typedef enum {
    MSG_TYPE_CONTROL = 0,      // 控制指令（MQTT/BLE）
    MSG_TYPE_BLE_CONFIG = 1,   // BLE 配网
} msg_type_t;

typedef struct {
    msg_source_t source;       // BLE / MQTT
    msg_type_t type;           // ★新增
    uint16_t data_len;
    uint8_t data[256];
} g_msg_queue_t;
```

**优势总结**:
1. 节省 8.5KB 内存
2. 代码复用率高
3. 消息有序处理
4. 易于扩展（增加 type 即可）

---

## 📞 下一步行动

### 如果需要实现协议

1. **修改 `common.h`**: 增加 `msg_type_t` 枚举
2. **修改 `use_ble_server.c`**: 支持 JSON 格式判断
3. **修改 `app_main.c`**: 增加配网处理逻辑
4. **测试**: 参考《协议设计文档.md》第11章

### 如果只是了解协议

- 查看 **《协议快速参考.md》** 获取速查表
- 查看 **《协议设计文档.md》** 获取完整规范
- 查看本文档理解设计决策

---

**文档版本**: v1.0  
**最后更新**: 2025-10-22

