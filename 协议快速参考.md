# ESP32-C5 协议快速参考卡片

## 🔑 核心原则

> **MQTT 和 BLE 的控制指令格式完全相同，都使用十六进制字符串**

```
统一格式: "0C0164000000002864"
         └─ 命令字 0x0C
            └─ 功能码和参数（根据主控盒协议定义）
```

---

## 📡 MQTT 协议速查

### 订阅 Topic（接收云端指令）
```
tylink/{deviceId}/thing/property/set
```

### 发布 Topic（上报设备状态）
```
tylink/{deviceId}/thing/property/report
```

### 控制指令格式
```json
{
    "msgId": "1234567890",
    "time": 1640000000000,
    "data": {
        "mccil": "0C0164000000002864"
    }
}
```

### 状态上报格式
```json
{
    "msgId": "9876543210",
    "time": 1640000001000,
    "data": {
        "online": true,
        "deviceStatus": {
            "position": 100,
            "mode": 2,
            "alarm": 0
        }
    }
}
```

---

## 📱 BLE 协议速查

### GATT 服务
```
服务 UUID: 59462f12-9543-9999-12c8-58b459a2712d
控制特征: 33333333-2222-2222-2222-111111110000 (Write)
状态特征: 33333333-2222-2222-2222-111111110001 (Notify)
```

### 方式1: JSON 格式（推荐）

#### 控制指令
```json
{
    "cmd": "control",
    "data": "0C0164000000002864"
}
```

#### 配网指令
```json
{
    "cmd": "config",
    "data": {
        "ssid": "MyWiFi",
        "password": "12345678"
    }
}
```

### 方式2: 直接写入（兼容当前实现）

```
直接写入十六进制字符串: "0C0164000000002864"
```

---

## 🎯 指令示例

### 开门指令
```
MQTT: {"data": {"mccil": "0C1000000000000000001C"}}
BLE:  {"cmd": "control", "data": "0C1000000000000000001C"}
```

### 关门指令
```
十六进制: "0C1100000000000000001D"
```

### 设置位置到 50%
```
十六进制: "0C010000003200050044"
解析: 0x0C=控制命令, 0x01=设置位置, 0x32=50(十进制), 0x05=速度
```

---

## 🔄 数据转换流程

```
┌──────────────────┐
│ MQTT/BLE 接收    │
│ "0C0164..."      │  十六进制字符串
└────────┬─────────┘
         │
         │ 每2个字符 → 1字节
         ▼
┌──────────────────┐
│ 字节数组         │
│ [0x0C, 0x01,     │
│  0x64, ...]      │
└────────┬─────────┘
         │
         │ UART 发送
         ▼
┌──────────────────┐
│ 主控盒处理       │
└──────────────────┘
```

---

## ⚙️ 配置参数

| 参数 | 值 | 说明 |
|------|-----|-----|
| MQTT Broker | `mqtts://m1.tuyacn.com:8883` | 涂鸦云地址 |
| UART 波特率 | 115200 | 8N1 |
| BLE MTU | 500 (首选) / 23 (默认) | 自动协商 |
| 消息队列深度 | 10 条 | 每条 256 字节 |

---

## 🚨 错误码速查

| Code | 说明 | 处理 |
|------|------|-----|
| 0 | 成功 | - |
| 1002 | JSON 解析失败 | 检查格式 |
| 1003 | mccil 字段缺失 | 添加该字段 |
| 1004 | 十六进制转换失败 | 检查字符是否合法 |
| 2003 | WiFi 配网失败 | 检查 SSID/密码 |
| 3004 | 未识别的命令字 | 当前只支持 0x0C |

---

## 📊 性能指标

| 指标 | 典型值 |
|------|--------|
| MQTT 延迟 | 200ms |
| BLE 延迟 | 50ms |
| UART 响应 | 10ms |
| MQTT 吞吐 | 10 条/秒 |

---

## ✅ 开发检查清单

### MQTT 控制
- [ ] 消息包含 `msgId`, `time`, `data` 字段
- [ ] `data.mccil` 是大写十六进制字符串
- [ ] 字符串长度是偶数
- [ ] 第一个字节是 0x0C

### BLE 控制
- [ ] JSON 格式包含 `cmd` 和 `data` 字段
- [ ] 或直接写入十六进制字符串
- [ ] 数据长度不超过 MTU-3

### BLE 配网
- [ ] `cmd` 字段为 `"config"`
- [ ] 包含 `ssid` 和 `password`
- [ ] SSID ≤ 32 字节，密码 ≤ 64 字节

---

## 💡 关键设计决策

### ✅ 为什么使用单队列？

1. **控制指令完全相同** - MQTT 和 BLE 都转换为十六进制发送串口
2. **串口是瓶颈** - 无论哪个来源，最终都要排队输出
3. **节省资源** - 单队列占用 2.5KB，双队列需要 5KB
4. **简化逻辑** - 统一处理，代码更清晰

### ✅ 为什么增加 type 字段？

1. **区分配网和控制** - BLE 需要处理 WiFi 配网
2. **易于扩展** - 未来可能增加 OTA、查询等类型
3. **语义清晰** - 不依赖数据内容判断

### ✅ 为什么 BLE 支持两种格式？

1. **向后兼容** - 当前代码直接处理十六进制字符串
2. **未来演进** - JSON 格式更灵活，支持配网等功能
3. **渐进式升级** - 两种格式共存，平滑过渡

---

## 🔧 实现要点

### ESP32-C5 端

```c
// 1. 接收 MQTT 消息
case MQTT_MANAGER_EVENT_DATA_RECEIVED:
    // 解析 JSON，提取 data.mccil
    cJSON *mccil_item = cJSON_GetObjectItem(data, "mccil");
    // 发送到队列
    msg.source = MSG_SOURCE_MQTT;
    msg.type = MSG_TYPE_CONTROL;
    xQueueSend(g_msg_queue, &msg, portMAX_DELAY);

// 2. 接收 BLE 消息
case BLE_GATT_ACCESS_OP_WRITE_CHR:
    // 判断是 JSON 还是纯十六进制
    if (is_json(data)) {
        // 解析 cmd 字段，设置 type
        msg.type = (cmd == "config") ? MSG_TYPE_CONFIG : MSG_TYPE_CONTROL;
    } else {
        // 兼容模式
        msg.type = MSG_TYPE_CONTROL;
    }
    xQueueSend(g_msg_queue, &msg, portMAX_DELAY);

// 3. 统一处理
void mqtt_ble_data_parser_cb(const g_msg_queue_t* msg) {
    if (msg->type == MSG_TYPE_CONFIG) {
        // 解析 WiFi 配网
        parse_wifi_config(msg->data);
        wifi_manager_connect(ssid, password);
    } else {
        // 十六进制字符串 → 字节数组
        hex_string_to_bytes(msg->data);
        // 发送串口
        uart_send_data(bytes, len);
    }
}
```

### 云端/APP 端

```javascript
// MQTT 发送（Node.js）
client.publish('tylink/xxx/thing/property/set', JSON.stringify({
    msgId: Date.now().toString(),
    time: Date.now(),
    data: { mccil: "0C0164000000002864" }
}));

// BLE 发送（JavaScript）
const command = { cmd: "control", data: "0C0164000000002864" };
await characteristic.writeValue(
    new TextEncoder().encode(JSON.stringify(command))
);
```

---

## 📞 技术支持

- **完整文档**: 见 `协议设计文档.md`
- **主控盒协议**: 见 `控制系统 RF&BLE 通信协议 V1.14.docx`
- **涂鸦云文档**: https://developer.tuya.com/docs/iot/tuyalink-intro

---

**最后更新**: 2025-10-22

