# 任务看门狗使用说明

## 概述

本项目已集成ESP32任务看门狗（Task Watchdog Timer, TWDT），用于监控关键任务，提高系统可靠性。

## 配置参数

- **超时时间**: 30秒
- **触发行为**: 超时后自动重启系统（panic重启）
- **监控任务**: 
  - 消息处理任务 (msg_task)
  - IoT业务任务 (iot_task)
  - UART接收任务 (uart_receive)

## 工作原理

### 1. 初始化
在 `main()` 函数中，系统启动时初始化看门狗：
```c
ESP_ERROR_CHECK(watchdog_init());
```

### 2. 任务订阅
每个被监控的任务在启动时订阅看门狗：
```c
esp_task_wdt_add(NULL);  // NULL表示当前任务
```

### 3. 定期喂狗
任务在运行循环中定期调用喂狗函数，告诉看门狗"我还活着"：
```c
esp_task_wdt_reset();
```

### 4. 超时处理
如果任务在30秒内没有喂狗，看门狗会触发系统panic重启，并打印错误信息。

## 关键设计

### 避免永久阻塞
所有被监控任务都使用**超时等待**而不是永久阻塞：

```c
// ❌ 错误：永久等待会导致看门狗超时
xQueueReceive(queue, &msg, portMAX_DELAY);

// ✅ 正确：10秒超时，保证能定期喂狗
xQueueReceive(queue, &msg, pdMS_TO_TICKS(10000));
esp_task_wdt_reset();
```

## 看门狗超时示例

如果任务卡死，系统会打印类似如下的错误信息：

```
E (34567) task_wdt: Task watchdog got triggered. The following tasks did not reset the watchdog in time:
E (34567) task_wdt:  - msg_task (CPU 0)
E (34567) task_wdt: Tasks currently running:
E (34567) task_wdt: CPU 0: IDLE
E (34567) task_wdt: Aborting.

Guru Meditation Error: Core  0 panic'ed (Watchdog timeout on CPU0)
```

系统会自动重启，并从错误中恢复。

## 好处

1. **自动恢复**: 系统死锁或任务卡死时自动重启
2. **无人值守**: 适合长期运行的IoT设备
3. **快速定位**: 错误日志会显示哪个任务出现问题
4. **提高可靠性**: 避免因软件bug导致设备"假死"

## 调试建议

### 禁用看门狗（调试时）
如果需要单步调试，可以临时注释掉看门狗初始化：
```c
// ESP_ERROR_CHECK(watchdog_init());  // 调试时注释掉
```

### 调整超时时间
如果任务正常执行但仍然超时，可以增加超时时间：
```c
#define WATCHDOG_TIMEOUT_SEC    60  // 改为60秒
```

### 查看崩溃日志
系统重启后，可以通过串口监视器查看崩溃原因：
```bash
idf.py monitor
```

## 最佳实践

1. **合理的喂狗频率**: 建议在主循环开始时喂狗
2. **避免长时间阻塞**: 使用超时代替永久等待
3. **监控关键任务**: 只监控重要的业务任务，不要监控所有任务
4. **适当的超时值**: 根据任务的最长执行时间设置

## 注意事项

⚠️ **不要在中断服务函数（ISR）中调用看门狗API**

⚠️ **长时间的计算任务需要拆分，并在循环中喂狗**

⚠️ **如果任务需要进行超过30秒的操作，请增加超时时间或取消订阅**

## 监控任务详情

| 任务名称 | 优先级 | 栈大小 | 主要功能 | 喂狗位置 |
|---------|--------|--------|----------|----------|
| msg_task | 6 | 6KB | 处理MQTT/BLE消息 | 消息循环开始 |
| iot_task | 5 | 2KB | 定期业务逻辑 | 每20秒循环 |
| uart_receive | 5 | 3KB | 接收UART数据 | 事件循环开始 |

## 扩展

如果需要监控其他任务，按以下步骤操作：

1. 在任务函数开始处添加：
```c
esp_task_wdt_add(NULL);
ESP_LOGI(TAG, "xxx任务已订阅看门狗");
```

2. 在任务循环中定期调用：
```c
esp_task_wdt_reset();
```

3. 确保任务不会永久阻塞（使用超时等待）

